"use strict";

const app = require("../../restaurants.js");
const chai = require("chai");
const expect = chai.expect;
var event, context;

describe("Tests create restaurant", function () {
  it("creates a restaurant", async () => {
    return true;
  });

  it("returns 400 if model not correct", async () => {
    return true;
  });
});
